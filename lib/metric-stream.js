// var Stream = require('stream').Transform;
// var Util = require('util');

// var internals = {};
// exports = module.exports = internals.MetricStream = function(fqn, options){
// 	this.fqn = fqn;
// 	Transform.call(this, options);
// };

// Util.inherits(internals.MetricStream, Transform); 

// internals.MetricStream.prototype._transform = function() {

// };
